upstream django-api {
    server django-api:8000;
}

server {
    listen 80;

    location / {
        include proxy_params;
        proxy_pass http://django-api ;
    }
    location /static/ {
        alias /itsoda/static/;
    }

    location /media/ {
        alias /itsoda/media/;
    }
}